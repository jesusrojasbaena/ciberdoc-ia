# CiberDoc IA — contexto para Claude Code

**Fuente de verdad:** [`docs/especificacion-ciberdoc.md`](docs/especificacion-ciberdoc.md). Leerlo entero al empezar cada sesión. No desviarse de él sin consultar antes con el usuario.

## Estado

- ✅ Tarea 1 (§11): repo inicializado, estructura de §9, `css/estilos.css` base con la paleta de §6, push a `main` con autoría de la cuenta GitHub `jesusrojasbaena`.
- ▶️ Próxima tarea: **tarea 2 (§11)** — pipeline de conversión Word→web (§5), `scripts/convertir-curso.mjs`.

## Regla innegociable

La carpeta fuente del curso vive en `C:\Proyectos\_fuentes\07_Entrega_Final` y **nunca** se copia dentro de este repositorio. Ningún solucionario, guía docente, prompt IA ni material de los módulos 2–8 puede acabar en el repo ni en el JSON publicado (`data/curso-ciberseguridad.json`). Verificar con `git status`/`git ls-files` antes de cada commit.

## Forma de trabajo

- Siempre en **Plan mode**.
- Una sola tarea del §11 por sesión.
- El usuario revisa manualmente antes de cada commit.
